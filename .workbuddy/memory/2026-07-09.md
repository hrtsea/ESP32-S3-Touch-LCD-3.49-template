# 2026-07-09

## 项目熟悉 + 最佳实践评审

对 ESP32-S3-Touch-LCD-3.49-template（NAS 监控屏固件）做了全面代码评审，产出 `BEST_PRACTICES_REVIEW.md`。

关键结论：
- 架构正确：事件总线解耦 + 数据源抽象工厂(VTable) + Octal SPIRAM/DMA 内存规划专业。
- **P0 生产化缺口**：
  1. Core Dump 关闭（`CONFIG_ESP_COREDUMP_ENABLE_TO_NONE`）→ 需开 to-flash + 分区。
  2. 无 OTA 分区（partitions.csv 只有 factory 8M）→ 需双 OTA + 回滚。
  3. `hw_init.c::system_monitor_start` 直接调 `heartbeat_loop()`（for(;;)）导致 app_main 永久阻塞，应改 xTaskCreate 独立任务。
- **P1 RTOS**：`nas_event_loop_stop` 用 vTaskDelete 强杀（可能持锁死锁）；event_bus 非递归锁有重入死锁隐患；NasData 全局缓冲+队列二重投递竞态。
- **P3 安全**：Synology 登录密码在 URL 明文（默认 http）；默认 AP 密码 12345678。
- 既有 REFACTOR_SUGGESTIONS.md / REFACTOR_SUMMARY.md 方向正确，但落地点应优先 P0 可恢复性。

注：用户问的"最佳时间建议"应为"最佳实践建议"（同音字 shijian）。

## Code Review：最新 commit 4628213（WiFi 配网超时 + bridge 模块）

用户用 /code_review 命令审查 `main/`（首次误指向 build/ 的 .obj 编译产物，已纠正）。工作树对 main/ 无未提交变更，按流程回退分析最新 commit。产出 `CODE_REVIEW_2026-07-09.md`。

核心发现（已用 event_bus.c / nas_event_loop.c / CMakeLists.txt 交叉验证）：
- **🔴 event_bus_publish 不拷贝 payload**（event_bus.c:124-150 存裸指针 + xQueueSend），bridge 转发 esp_bus 临时指针 → 脆弱契约。当前 nas_event_loop 只 switch(evt.id) 不解引用，故不崩，但重构即炸。建议桥接拷进静态缓冲或与 publish_nas_data 对齐。
- **🟠 user_config.h 弱默认凭证入库**（12345678 / admin/admin，#ifndef 可覆盖，但漏配即出厂弱密码）。
- **🟠 wifi_connect 删了 s_last_reason=0 复位** → 陈旧断连原因残留到新连接。
- **🟡 WIFI_PROV_ON_FAILURE + 重连耗尽重启** 恢复路径需真机验证 SoftAP 出现时间。
- **🟡 main/ui/old/ 仍引用已删 API**（wifi_get_curr_ssid/wifi_reason_str 共 8 处），未编译但死代码陷阱。
- 肯定：凭证存储从自定义 NVS 迁移到 esp_wifi_config（wifi_cfg_get_network 等），去轮询改事件驱动，方向正确。
